generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model Commodity {
  id         Int      @id @default(autoincrement())
  category   String?
  name       String
  price      Float?
  region     String   @default("NCR")
  sourceDate DateTime
  createdAt  DateTime @default(now())

  @@unique([name, region, sourceDate])
  @@index([name, sourceDate])
}

model Recipe {
  id           Int                 @id @default(autoincrement())
  title        String
  url          String              @unique
  imageUrl     String?
  description  String?             @db.Text
  servings     Int?
  prepTime     Int?
  cookTime     Int?
  sourceSite   String              @default("PanlasangPinoy")
  scrapedAt    DateTime            @default(now())
  ingredients  RecipeIngredient[]
  instructions RecipeInstruction[]
}

model RecipeIngredient {
  id       Int    @id @default(autoincrement())
  recipeId Int
  recipe   Recipe @relation(fields: [recipeId], references: [id], onDelete: Cascade)
  rawText  String
  name     String
  amount   String?
  unit     String?

  @@index([recipeId])
}

model RecipeInstruction {
  id         Int    @id @default(autoincrement())
  recipeId   Int
  recipe     Recipe @relation(fields: [recipeId], references: [id], onDelete: Cascade)
  stepNumber Int
  text       String @db.Text

  @@unique([recipeId, stepNumber])
}

model NutritionFact {
  id            Int      @id @default(autoincrement())
  foodName      String
  foodCode      String   @unique
  calories      Float?
  protein       Float?
  fat           Float?
  carbohydrates Float?
  fiber         Float?
  calcium       Float?
  iron          Float?
  vitaminA      Float?
  vitaminB1     Float?
  vitaminB2     Float?
  vitaminC      Float?
  sourceUrl     String?
  source        String   @default("FNRI")
  scrapedAt     DateTime @default(now())
}

model StoreProduct {
  id         Int      @id @default(autoincrement())
  sku        String
  name       String
  category   String?
  uom        String?
  price      Float?
  imageUrl   String?
  url        String?
  source     String   @default("SMMarkets")
  sourceDate DateTime
  createdAt  DateTime @default(now())

  @@unique([sku, sourceDate])
  @@index([sku, sourceDate])
}

model ScrapingLog {
  id            Int       @id @default(autoincrement())
  type          String
  status        String
  itemsScraped  Int       @default(0)
  message       String?   @db.Text
  cursor        String?   @db.Text
  startedAt     DateTime  @default(now())
  finishedAt    DateTime?
}
